import { createSlice } from '@reduxjs/toolkit';

const initialState = {
};

export const comparsaSlice = createSlice({
  name: 'comparsa',
  initialState,
  reducers: {
  }
});

export const { } = comparsaSlice.actions;
export const comparsaReducer = comparsaSlice.reducer;
